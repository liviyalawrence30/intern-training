export interface Intern {
  id: number
  name: string
  score: number
  role: string
  isPresent: boolean
}

export interface InternFormState {
  name: string
  score: number
  role: string
  isPresent: boolean
}