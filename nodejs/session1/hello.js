const message = "Hello from Node.js"
console.log(message)

const fruits = ["apple", "banana", "mango"]
const upper = fruits.map(f => f.toUpperCase())
console.log(upper)
// Browser JavaScript runs inside a web browser and can interact with the DOM.
// Node.js runs outside the browser, does not have a DOM, and can access the file system and other server resources.

//REPL stands for Read-Eval-Print Loop. It is an interactive programming environment that takes single user inputs , evaluates them, and returns the result to the user. 
//The REPL is a great way to experiment with Node.js code and test out ideas quickly. 
